**.networkLayerType = "LEACH_DCHS_Layer"
**.np.percentage = sqrt(1/((${numSensors})*3.14159265359)) * (2 / 0.765)
**.np.roundLength = ${sendInterval} * ${roundLengthMult = 1, 2, 3}s
**.np.aggrCoefficient = ${aggrCoeff = 0.05, 1}
